extends ../layout

block content

  - if (null !== {{{name}}})

    h2.page-headline Edit {{{name}}} details
      .pull-right
        include _operations

    br
    include _form

  - else

    - var entity = '{{{model}}}'
    include ../error/notfound

