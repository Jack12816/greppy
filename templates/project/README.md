# ACME Application

## Dependencies

* Node.js (pacman -S nodejs)
* Bower (npm install -g bower)

## Installation

Just run these commands to setup all dependencies:

    npm install
    bower install

